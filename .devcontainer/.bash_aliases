# Add anything here that should be available to new bash sessions

# Disable git status in bash prompts. Remove if you hate speed
git config --global codespaces-theme.hide-status 1

alias ll='ls -lA'
alias la='ls -A'
alias l='ls -CF'
